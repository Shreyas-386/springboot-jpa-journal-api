spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Project
    username: root
    password: camb472@3#

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8081